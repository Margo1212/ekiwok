import { EmojiHappyIcon, UserIcon } from '@heroicons/react/outline';
import type { NextPage } from 'next';
import Head from 'next/head';

import Input from '../components/Input';
import styles from '../styles/Home.module.css';

const Home: NextPage = () => {
  return (
    <div className={styles.container}>
      <Head>
        <title>Ekiwok</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/noto_melting-facefavicon-emoji.png" />
      </Head>
      <Input
        placeholder="USER12345"
        icon={<UserIcon className="h-5 w-5 text-border" />}
        iconPosition="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none"
      />
      <Input
        placeholder=""
        icon={<EmojiHappyIcon className="h-5 w-5 text-border" />}
        iconPosition="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none"
      />
    </div>
  );
};

export default Home;
